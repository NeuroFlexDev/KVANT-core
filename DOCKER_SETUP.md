# Docker Setup - Kvant Core

Полная документация по запуску проекта с помощью Docker Compose для локального тестирования и разворачивания.

## 🚀 Быстрый старт

### Для разработки:

```bash
# 1. Склонировать проект и перейти в директорию
cd kvant-core

# 2. Настроить окружение
make setup

# 3. Запустить в режиме разработки
make dev
```

Приложение будет доступно:
- **Фронтенд:** http://localhost:5173
- **Бэкенд API:** http://localhost:8000
- **API Документация:** http://localhost:8000/api/docs
- **База данных:** localhost:5432

### Для продакшена:

```bash
# 1. Настроить production окружение
cp .env.prod .env
# Отредактировать .env с реальными значениями

# 2. Запустить в режиме продакшена
make prod
```

## 📁 Структура файлов

```
kvant-core/
├── docker-compose.yml          # Универсальный compose (не рекомендуется для прямого использования)
├── docker-compose.dev.yml      # Для разработки
├── docker-compose.prod.yml     # Для продакшена
├── Makefile                     # Команды для управления
├── .env.example                 # Пример конфигурации
├── .env.dev                     # Настройки для разработки
├── .env.prod                    # Настройки для продакшена
├── kvant/
│   ├── Dockerfile              # Dockerfile для фронтенда
│   ├── nginx.conf              # Nginx конфигурация для фронтенда
│   └── .dockerignore
├── omi-dev/
│   ├── Dockerfile              # Dockerfile для бэкенда
│   └── .dockerignore
└── nginx/
    └── nginx.prod.conf         # Nginx конфигурация для продакшена
```

## 🛠 Команды Makefile

### Основные команды:

| Команда | Описание |
|---------|----------|
| `make help` | Показать справку по всем командам |
| `make setup` | Первоначальная настройка проекта |
| `make dev` | Запустить в режиме разработки |
| `make prod` | Запустить в режиме продакшена |
| `make down` | Остановить все контейнеры |
| `make logs` | Показать логи всех сервисов |

### Команды для разработки:

```bash
make dev-detached          # Запустить в фоне
make restart-backend       # Перезапустить только бэкенд
make restart-frontend      # Перезапустить только фронтенд
make shell-backend         # Войти в контейнер бэкенда
make shell-frontend        # Войти в контейнер фронтенда
make shell-db             # Войти в базу данных
```

### Команды для сборки:

```bash
make build                # Собрать все образы
make build-dev            # Собрать для разработки
make build-prod           # Собрать для продакшена
```

### Команды для очистки:

```bash
make clean                # Очистить неиспользуемые ресурсы
make clean-all            # Полная очистка (ОПАСНО!)
make down-v               # Остановить и удалить volumes
```

## 🔧 Конфигурация

### Переменные окружения (.env)

#### Основные настройки:
- `APP_ENV` - Режим приложения (dev/prod)
- `BUILD_TARGET` - Цель сборки Docker (development/production)
- `NODE_ENV` - Режим Node.js (development/production)

#### Порты:
- `FRONTEND_PORT` - Порт фронтенда (по умолчанию 5173)
- `BACKEND_PORT` - Порт бэкенда (по умолчанию 8000)
- `DB_PORT` - Порт базы данных (по умолчанию 5432)

#### База данных:
- `DB_HOST` - Хост базы данных
- `DB_NAME` - Имя базы данных
- `DB_USER` - Пользователь базы данных
- `DB_PASSWORD` - Пароль базы данных

#### Безопасность:
- `SECRET_KEY` - Секретный ключ приложения
- `DEBUG` - Режим отладки (true/false)

### Переключение между режимами:

```bash
# Переключиться на разработку
cp .env.dev .env
make dev

# Переключиться на продакшен
cp .env.prod .env
make prod
```

## 🐳 Docker Services

### Frontend (React + Vite)
- **Development:** Hot reload, исходники монтируются как volumes
- **Production:** Собранная статика, оптимизированная сборка
- **Nginx:** Автоматическое проксирование API запросов

### Backend (FastAPI + Python)
- **Development:** Hot reload, автоматическая перезагрузка при изменениях
- **Production:** Оптимизированная сборка без dev зависимостей
- **Healthcheck:** Автоматическая проверка работоспособности

### Database (PostgreSQL)
- **Автоматическая инициализация** при первом запуске
- **Persistent volumes** для сохранения данных
- **Healthcheck** для проверки готовности

### Nginx (только для продакшена)
- **Load balancing** между сервисами
- **SSL termination** (настраивается)
- **Static files serving** с кэшированием
- **API proxying** с таймаутами

### Redis (опционально)
- **Кэширование** для улучшения производительности
- **Session storage** для пользовательских сессий
- Включается с профилем `redis`

## 🔍 Отладка и мониторинг

### Просмотр логов:

```bash
# Все сервисы
make logs

# Конкретный сервис
make logs-backend
make logs-frontend
make logs-db
```

### Проверка статуса:

```bash
# Статус контейнеров
make ps

# Процессы в контейнерах
make top
```

### Подключение к сервисам:

```bash
# Терминал бэкенда
make shell-backend

# Терминал фронтенда
make shell-frontend

# Подключение к базе данных
make shell-db
```

### Healthchecks:

- **Backend:** http://localhost:8000/api/docs
- **Frontend:** http://localhost:5173 (dev) или http://localhost:80 (prod)
- **Database:** PostgreSQL ready check

## 💾 Работа с базой данных

### Бэкап и восстановление:

```bash
# Создать бэкап
make db-backup

# Восстановить из бэкапа
make db-restore FILE=backup_20241022_120000.sql
```

### Миграции:

```bash
# Выполнить миграции
make db-migrate
```

### Прямое подключение:

```bash
# Через Docker
make shell-db

# Напрямую (если PostgreSQL клиент установлен)
psql -h localhost -p 5432 -U kvant_user -d kvant_dev
```

## 🚀 Развертывание в продакшене

### 1. Подготовка сервера:

```bash
# Установить Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo usermod -aG docker $USER

# Установить Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

### 2. Настройка окружения:

```bash
# Скопировать и настроить .env.prod
cp .env.prod .env

# Обязательно изменить:
# - DB_PASSWORD (сильный пароль)
# - SECRET_KEY (случайная строка)
# - EMAIL настройки
# - AWS настройки
# - SENTRY_DSN для мониторинга
```

### 3. Настройка SSL (рекомендуется):

```bash
# Создать директорию для SSL сертификатов
mkdir -p nginx/ssl

# Разместить сертификаты
# nginx/ssl/cert.pem
# nginx/ssl/key.pem

# Раскомментировать HTTPS блок в nginx/nginx.prod.conf
```

### 4. Запуск:

```bash
# Запустить в продакшене
make prod

# Проверить статус
make ps
make logs
```

## 🔒 Безопасность

### Рекомендации для продакшена:

1. **Смените все пароли** в `.env.prod`
2. **Используйте SSL сертификаты** для HTTPS
3. **Настройте firewall** на сервере
4. **Регулярно создавайте бэкапы** базы данных
5. **Мониторьте логи** на предмет подозрительной активности
6. **Обновляйте Docker образы** для получения security патчей

### Переменные которые ОБЯЗАТЕЛЬНО нужно изменить:

- `DB_PASSWORD` - пароль базы данных
- `SECRET_KEY` - секретный ключ приложения
- `MAIL_HOST_PASSWORD` - пароль email
- `AWS_KEY_ID` и `AWS_SECRET` - AWS credentials
- `SENTRY_DSN` - для мониторинга ошибок

## 🆘 Устранение проблем

### Частые проблемы:

1. **Порт уже занят:**
   ```bash
   make down
   # Изменить порт в .env файле
   make dev
   ```

2. **База данных не запускается:**
   ```bash
   make down-v  # Удалит все данные!
   make dev
   ```

3. **Ошибки сборки:**
   ```bash
   make clean
   make build-dev
   ```

4. **Проблемы с правами:**
   ```bash
   sudo chown -R $USER:$USER .
   ```

### Полный сброс (удалит все данные):

```bash
make down-v
make clean-all
make setup
make dev
```

## 📚 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Vite Documentation](https://vitejs.dev/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

**Поддержка:** Если возникли проблемы, проверьте логи с помощью `make logs` и обратитесь к разделу устранения проблем выше.