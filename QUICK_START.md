# 🚀 Быстрый старт Kvant Core

## Пререквизиты

- Docker и Docker Compose установлены
- Make установлен (обычно есть в Linux по умолчанию)

## 🔧 Первоначальная настройка

### 1. Настройка прав Docker (обязательно!)

```bash
# Настроить права Docker
make docker-setup

# Перезайти в систему или выполнить
newgrp docker

# Проверить что Docker работает без sudo
docker ps
```

### 2. Настройка проекта

```bash
# Создать .env файл
make setup
```

## 🏃‍♂️ Запуск

### Вариант 1: Обычный запуск (рекомендуется)

```bash
# Запустить в режиме разработки
make dev
```

### Вариант 2: Если есть проблемы с правами

```bash
# Запустить с sudo
make dev-sudo
```

### Вариант 3: В фоновом режиме

```bash
# Запустить в фоне
make dev-detached

# Посмотреть логи
make logs
```

## 🌐 Доступ к сервисам

После успешного запуска:

- **Фронтенд:** http://localhost:5173
- **Бэкенд API:** http://localhost:8000
- **API Документация:** http://localhost:8000/api/docs
- **База данных:** localhost:5432

## 🔍 Проверка работоспособности

```bash
# Статус контейнеров
make ps

# Логи всех сервисов
make logs

# Логи конкретного сервиса
make logs-backend
make logs-frontend
```

## 🛑 Остановка

```bash
# Остановить все сервисы
make down

# Остановить и удалить volumes (все данные)
make down-v
```

## 🆘 Если что-то не работает

### 1. Проблемы с Docker правами

```bash
# Проверить группы пользователя
groups $USER

# Если нет группы docker - выполнить
make docker-setup
# Затем перезайти в систему
```

### 2. Порт уже занят

```bash
# Изменить порт в .env файле
FRONTEND_PORT=3001
BACKEND_PORT=8001

# Или остановить конфликтующие сервисы
sudo lsof -i :5173
sudo lsof -i :8000
```

### 3. Ошибки сборки

```bash
# Очистить Docker кэш
make clean

# Пересобрать
make build-dev
```

### 4. Проблемы с базой данных

```bash
# Полная перезагрузка БД (удалит все данные!)
make down-v
make dev
```

## 📚 Дополнительные команды

```bash
# Показать все команды
make help

# Подключиться к контейнеру бэкенда
make shell-backend

# Подключиться к базе данных
make shell-db

# Создать бэкап БД
make db-backup

# Очистить неиспользуемые ресурсы
make clean
```

## 🔧 Альтернативный запуск без Docker

Если Docker не работает, можно запустить напрямую:

### Бэкенд:
```bash
cd omi-dev
pip install poetry
poetry install
poetry shell
export PYTHONPATH=$PWD/src
uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```

### Фронтенд:
```bash
cd kvant
npm install
npm run dev
```

### База данных:
```bash
# Только PostgreSQL в Docker
docker run --name kvant-postgres \
  -e POSTGRES_DB=kvant_dev \
  -e POSTGRES_USER=kvant_user \
  -e POSTGRES_PASSWORD=kvant_password \
  -p 5432:5432 -d postgres:13-alpine
```

---

**Поддержка:** При проблемах смотрите [DOCKER_TROUBLESHOOTING.md](DOCKER_TROUBLESHOOTING.md) или [DOCKER_SETUP.md](DOCKER_SETUP.md)