# Kvant Core

Веб-приложение для работы с анкетами и формами. Состоит из React фронтенда и FastAPI бэкенда.

## 🚀 Быстрый старт

### С Docker (рекомендуется):

```bash
# 1. Настроить права Docker (обязательно!)
make docker-setup
# Затем перезайти в систему или выполнить: newgrp docker

# 2. Первоначальная настройка
make setup

# 3. Запуск в режиме разработки
make dev

# Если есть проблемы с правами Docker:
make dev-sudo
```

**Приложение будет доступно:**
- Фронтенд: http://localhost:5173
- Бэкенд: http://localhost:8000
- API Docs: http://localhost:8000/api/docs

### Без Docker:

#### Бэкенд:
```bash
cd omi-dev
pip install poetry
poetry install
poetry shell
uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```

#### Фронтенд:
```bash
cd kvant
npm install
npm run dev
```

## 📚 Документация

- **[Быстрый старт](QUICK_START.md)** - пошаговое руководство для немедленного запуска
- **[Полная документация Docker](DOCKER_SETUP.md)** - подробное руководство по развертыванию
- **[Устранение проблем](DOCKER_TROUBLESHOOTING.md)** - решения частых проблем
- **[Синхронизация API](BACKEND_FRONTEND_SYNC.md)** - информация о синхронизации бэкенда и фронтенда

## 🛠 Основные команды

| Команда | Описание |
|---------|----------|
| `make help` | Показать все доступные команды |
| `make dev` | Запустить в режиме разработки |
| `make prod` | Запустить в режиме продакшена |
| `make down` | Остановить все сервисы |
| `make logs` | Показать логи |
| `make clean` | Очистить неиспользуемые ресурсы |

## 🏗 Архитектура

- **Фронтенд:** React + TypeScript + Vite
- **Бэкенд:** FastAPI + Python + SQLAlchemy
- **База данных:** PostgreSQL
- **Контейнеризация:** Docker + Docker Compose

## 🔗 Доступные сервисы

После запуска `make dev`:

- **Фронтенд:** http://localhost:5173
- **Бэкенд API:** http://localhost:8000
- **API Документация:** http://localhost:8000/api/docs
- **База данных:** localhost:5432

## 📝 Разработка

### Структура проекта:

```
kvant-core/
├── kvant/                  # React фронтенд
│   ├── src/
│   ├── public/
│   └── Dockerfile
├── omi-dev/               # FastAPI бэкенд
│   ├── src/
│   ├── pyproject.toml
│   └── Dockerfile
├── docker-compose.dev.yml # Развертывание для разработки
├── docker-compose.prod.yml # Развертывание для продакшена
└── Makefile              # Команды для управления
```

### API эндпоинты:

- `POST /api/select-card` - выбор карточек
- `POST /api/update-field` - обновление полей форм
- `POST /test` - тестовый эндпоинт
- `GET /api/docs` - документация API

## 🔧 Настройка

### Переменные окружения:

Основные настройки находятся в `.env` файлах:

- `.env.dev` - для разработки
- `.env.prod` - для продакшена (требует настройки)

### CORS настройки:

Бэкенд настроен для работы с:
- `https://kvant.id` (продакшен)
- `http://localhost:5173` (разработка)
- `http://localhost:3000` (альтернативный порт)

## 🚀 Развертывание

### Разработка:
```bash
make dev
```

### Продакшен:
```bash
# 1. Настроить .env.prod
cp .env.prod .env
# Отредактировать переменные

# 2. Запустить
make prod
```

## 🆘 Устранение проблем

### Частые проблемы:

1. **Порт занят:** измените порт в `.env` файле
2. **Ошибки БД:** выполните `make down-v` для пересоздания
3. **Проблемы сборки:** выполните `make clean` и пересоберите

### Логи:
```bash
make logs              # Все сервисы
make logs-backend      # Только бэкенд
make logs-frontend     # Только фронтенд
```

### Подключение к контейнерам:
```bash
make shell-backend     # Терминал бэкенда
make shell-frontend    # Терминал фронтенда
make shell-db         # База данных
```

## 📄 Лицензия

[Укажите лицензию проекта]

---

Для подробной информации смотрите [DOCKER_SETUP.md](DOCKER_SETUP.md)